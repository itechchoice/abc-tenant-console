---
description: 组件拆分与文件体量约束，所有页面与组件生成都必须遵守
alwaysApply: true
---

# 组件拆分规则（强执行）

## 执行时机

- **前置执行，不是收尾整理**：在生成任何页面、组件、JSX 之前，必须先规划文件边界与组件层级；**禁止**先生成单个巨型文件，再在最后做机械拆分
- 修改旧文件时，若目标文件已超过下方硬阈值，且本次改动会继续增加复杂度，必须先拆分再继续实现功能

## 核心原则

- 拆分的首要目标是**降低人类阅读成本**，不是机械追求“组件越多越好”
- 优先保持**一个文件只承载一种主要职责**
- 拆分必须优先按**职责边界**进行，禁止为了缩短文件而拆出没有独立语义的薄包装组件
- 页面入口文件负责**组装**；复杂逻辑、局部视图、静态配置、纯函数应按职责下沉

## 行数阈值（硬约束）

- `src/pages/**/index.jsx`
  - 目标不超过 **220 行**
  - 超过 **280 行** 必须拆分
- `src/pages/**/components/*.jsx`
  - 目标不超过 **260 行**
  - 超过 **360 行** 必须拆分
- `src/components/**/*.jsx`
  - 目标不超过 **220 行**
  - 超过 **320 行** 必须拆分
- 复杂交互容器（如 Canvas、ChatMain、富交互面板、可视化编辑器）
  - 目标不超过 **320 行**
  - 超过 **450 行** 必须升级为同名目录并分层拆分
  - 超过 **600 行** 视为架构问题，**禁止**继续在单文件内追加功能

## 必须拆分的触发条件

满足任一条件即必须拆分：

- 文件超过对应硬阈值
- 文件中出现 **3 个及以上** 局部子组件
- 文件同时包含 **静态常量/配置 + 纯工具函数 + 主组件**
- 文件同时包含 **数据同步逻辑 + 复杂输入交互 + 大段展示 JSX**
- 阅读主组件时，需要跨越超过 **2 屏代码** 才能看完主要渲染结构

## 强制拆分顺序

拆分时必须优先按以下顺序处理：

1. 先抽离**静态配置**
   - 例如：状态映射、图标映射、蓝图数据、样式 token、选项常量
2. 再抽离**纯函数**
   - 例如：格式化函数、派生函数、selector、构图函数
3. 再抽离**第三方库适配层**
   - 例如：React Flow 视口容器、Markdown 渲染适配、滚动同步逻辑
4. 再抽离**有独立语义的局部视图**
   - 例如：`EmptyState`、`Skeleton`、`Timeline`、`Inspector`、`Toolbar`、`Filters`
5. 最后保留一个**薄的组装入口**
   - 入口文件只负责取状态、拼装子组件、下发必要 props

## 允许拆分的组件类型

以下类型通常有稳定语义，允许单独拆分：

- `EmptyState`
- `Skeleton`
- `Header`
- `Toolbar`
- `Timeline`
- `Inspector`
- `Filters`
- `Stats`
- `Table`
- `FormSection`
- `Viewport`
- `CanvasLayer`

## 禁止拆分的情况

以下情况**禁止**新建独立文件：

- 只是把 **5 到 15 行 JSX** 原样搬出去
- 组件只做一层 `<div>{children}</div>` 包装，且没有独立语义
- 组件没有自己的状态、样式复杂度、交互复杂度、领域语义，只是为了“让文件变短”
- 同一个父组件被拆成 `Header`、`Body`、`Content`、`Wrapper` 这类空泛命名，但每个文件都很薄

## 目录约束

- 页面专属拆分文件必须放到当前页面自己的 `components/` 下
- 只有组件被多个页面复用时，才允许提升到 `src/components/`
- 复杂全局组件允许使用**同名目录**收纳分层文件，例如：

```text
src/components/Workflow/WorkflowCanvas/
  index.jsx
  config.js
  graph.js
  FlowViewport.jsx
  ExecutionTimeline.jsx
```

## 执行判断标准

拆分前必须先判断以下 3 个问题，只有全部回答“是”才允许拆出独立文件：

1. 这个片段是否有稳定语义？
2. 抽出去后，主文件是否明显更短、更清晰？
3. 抽出去后，新文件是否仍然有足够职责，而不是薄包装？

## 默认执行策略

- 新增页面或复杂组件时，默认先规划目录与组件边界，再开始生成代码
- 如果页面专属组件只服务当前页面，必须先留在页面目录，不要提前放进全局 `src/components/`
- 如果现有文件已经超过硬阈值，后续新增功能时必须优先拆分，不要继续累积
