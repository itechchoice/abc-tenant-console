---
description: 纯 JSX 环境下基于 Zod 和 JSDoc 的类型推断与数据安全规范
globs: ["src/**/*.js", "src/**/*.jsx"]
---

# 纯 JSX 类型与数据验证架构规范

## 核心架构决策 (Architecture Decision)
本项目明确规定**不使用 TypeScript**。为了弥补纯 JSX 在复杂业务（如 AI 数据流、复杂表单、工作流配置）中带来的类型缺失与重构风险，我们确立以下**唯一最佳实践**：
**以 Zod Schema 为核心数据结构定义（SSOT），以 JSDoc 为类型推导工具。**

## 详细约束与 AI Agent 行为准则

### 1. Zod 作为唯一事实来源 (Single Source of Truth)
- 项目中所有重要的数据结构（大模型返回的 JSON、工作流节点配置、复杂的 API 入参），必须首先在 `src/schemas/` 目录下定义为 Zod Schema。
- 在对大模型返回的文本进行 `JSON.parse` 之后，或者从远端加载复杂配置时，必须立刻调用对应的 `schema.parse()` 或 `schema.safeParse()` 进行运行时数据清洗与拦截。

### 2. 强制 JSDoc 标注 (类型推导)
为了让 VS Code / Cursor 能够在使用纯 JS 时依然拥有完美的代码提示（IntelliSense），在编写核心工具函数或复杂组件时，必须使用 JSDoc 提取 Zod 的类型。

**Agent 代码生成示例规范：**
```javascript
import { z } from 'zod';

// 1. 定义 Schema
export const ToolCallSchema = z.object({
  action: z.string(),
  params: z.record(z.any())
});

/** * 2. 强制使用 JSDoc 推导出结构 
 * @typedef {z.infer<typeof ToolCallSchema>} ToolCallData 
 */

/**
 * 3. 在函数参数中使用标注
 * @param {ToolCallData} data 
 */
export function processToolCall(data) {
  // IDE 此时能完美推断出 data.action 和 data.params
  console.log(data.action);
}
```

### 3. 组件 Props 的防线
在编写复用型 UI 组件时，如果没有 PropTypes，必须在组件顶部使用严格的 JSDoc 注释标明入参结构，以防止团队成员错传 Props 导致渲染异常。

### 4. shadcn/ui 的降维实现规范
当需要引入 shadcn 组件时，**严禁运行 `npx shadcn add`**，必须参考其 TypeScript 源码，将其彻底转化为纯 JSX（`.jsx`）。转化时须遵守以下约束：

- **保留 `cn()` 工具函数**：必须使用 `clsx` + `tailwind-merge` 的组合（即 `src/lib/utils.js` 中导出的 `cn`）处理动态类名拼接，不可省略或自行替换
- **完整 JSDoc 类型声明**：组件顶部必须使用严格的 JSDoc 注释，标明 `variant`、`size` 等所有 Props 及其可选值，确保 IDE 拥有完美的代码补全与类型提示
- **移除所有 TS 语法**：删除 `interface`、`type`、泛型参数、`as const` 等 TypeScript 专属语法，用 JSDoc `@typedef` 和 `@param` 替代
- **存放位置**：统一存放在 `src/components/ui/` 目录下，文件名使用小写连字符（如 `button.jsx`、`dialog.jsx`）

**Agent 代码生成示例规范：**
```javascript
import { forwardRef } from 'react';
import { cva } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const buttonVariants = cva(
  'inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors focus-visible:outline-none disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        outline: 'border border-input bg-background hover:bg-accent',
        ghost: 'hover:bg-accent hover:text-accent-foreground',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm: 'h-9 rounded-md px-3',
        lg: 'h-11 rounded-md px-8',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: { variant: 'default', size: 'default' },
  },
);

/**
 * @param {object} props
 * @param {'default'|'outline'|'ghost'} [props.variant='default']
 * @param {'default'|'sm'|'lg'|'icon'} [props.size='default']
 * @param {string} [props.className]
 * @param {React.Ref<HTMLButtonElement>} ref
 */
const Button = forwardRef(({ className, variant, size, ...props }, ref) => (
  <button
    className={cn(buttonVariants({ variant, size, className }))}
    ref={ref}
    {...props}
  />
));
Button.displayName = 'Button';

export { Button, buttonVariants };
```