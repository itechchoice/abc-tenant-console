# utils 模块说明

`src/utils/` 存放带有明确业务辅助属性、但又不适合进入 store / hook / component 的工具函数。

## 当前文件

- `safeParser.ts`
  - 用于安全解析 AI 返回的 JSON 字符串
  - 支持可选的 Zod schema 校验
  - 保证函数本身不抛异常，而是返回结构化成功/失败结果

## 典型用途

- 解析模型生成的 JSON
- 解析工具返回的字符串化对象
- 在渲染前进行兜底校验，避免白屏

## 设计原则

对于不可信输入，优先返回结构化错误，而不是直接抛异常中断渲染流程。
